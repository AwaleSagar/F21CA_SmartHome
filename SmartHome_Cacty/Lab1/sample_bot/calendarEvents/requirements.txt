# Direct dependencies of the Google Calendar helper (read_events.py,
# setting_google_api.py).
#
# Derived from the imports actually present here: googleapiclient (from
# google-api-python-client) and google_auth_oauthlib. The previous contents
# pinned 22 packages, 20 of which were transitive dependencies of these two
# (cachetools, certifi, chardet, google-api-core, googleapis-common-protos,
# httplib2, idna, oauthlib, protobuf, pyasn1, pytz, requests, rsa, six,
# uritemplate, urllib3, ...). Pinning transitive dependencies at their 2020
# versions is what pulled in a large number of advisories; pip resolves them
# correctly from the two direct requirements below.

google-api-python-client
google-auth-oauthlib
