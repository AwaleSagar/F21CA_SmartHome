# Direct dependencies of the CACTY client (CACTY.py / CACTY_Windows_version.py,
# rasa_implementation.py, test_nlu.py).
#
# Derived from the imports actually present in this directory. The previous
# contents of this file were a `pip freeze` dump of a whole Anaconda install;
# that dump is kept for the record in environment-2020-frozen.pip-freeze.
#
# Versions are left unpinned where any maintained release works, so that a
# fresh install is not forced onto 2020 packages with known advisories.
# Rasa is the exception - see the note below.

# --- Conversational core ---
# Pinned to the 1.x line: this project is built against the Rasa 1.x API
# (SmartHome_NLP_Core/config.yml, data/nlu.md, data/stories.md are all
# Rasa 1.x format) and does not run on Rasa 2.x or 3.x. Rasa 1.6.1 is the
# version the coursework was marked with. This pin still carries known
# advisories that have no fix within the 1.x line.
rasa==1.6.1
rasa-sdk==1.6.1

# --- Speech in / out ---
SpeechRecognition
gTTS
playsound

# --- UI and media ---
pygame
Pillow

# --- NLP / numerics ---
nltk
numpy

# --- HTTP ---
requests

# NOTE: tkinter is used by CACTY.py but is part of the standard library.
# On Debian/Ubuntu it ships separately - install the python3-tk system package.
# NOTE: CACTY.py also shells out to ./ngrok; see README.md for how to obtain it.
